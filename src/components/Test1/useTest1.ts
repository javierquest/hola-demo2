import { useEffect } from 'react';

const useTest1 = () => {
  
  
  useEffect(() => {
  },[]);
  let data: any = {};
  const handle = (): any => {};

  let fns: any = {handle};
  return {data, fns};
}

export default useTest1;
