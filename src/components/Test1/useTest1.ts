import { useEffect } from 'react';

const useTest1 = () => {
  
  
  useEffect(() => {
  },[]);
  let data: any = {};
  const handle = (): any => {};

  const click = (): any => {};
  let fns: any = {handle, click};
  return {data, fns};
}

export default useTest1;
